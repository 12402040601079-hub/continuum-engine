services:
  - type: web
    name: continuum-engine
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    healthCheckPath: /api/v1/health
    envVars:
      - key: APP_VERSION
        value: 1.0.1

